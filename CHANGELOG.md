# Changelog

本项目的重要变更记录在此文件中。格式参考 Keep a Changelog；项目尚未建立正式版本发布流程，因此当前变更先记录在 `Unreleased`。

## [Unreleased]

### Added

- 本地资源新增统一分类倒排索引，以“分类类型 -> 具体标签 -> GID 集合”管理分类、播放列表和树状归类成员；“显示全部”复用同一索引的 GID 全集，父归类按后代节点合并，单本增量刷新和标签操作会同步更新索引。
- 在线资源搜索栏新增日期定位入口，使用 EH/EX 列表页原生 Seek 按 `YYYY-MM-DD` 跳到指定时间；日期可与当前关键词组合，E-Hentai 与 ExHentai 分别保存日期、游标和滚动状态，定位后可继续向新/旧方向翻页。
- 在线资源新增与本地标题列表同为 116px 高的“精简列表”视图，只显示标题、画廊类型、发布者和页数；Card / 精简列表 / Extended 三种模式可循环切换并记忆，精简列表复用 Compact 字段且不加载封面。
- 在线资源搜索栏旁新增画廊网址跳转入口：链接按钮展开独立输入框，只接受完整 EH/EX HTTPS `/g/{gid}/{token}/` 地址；提取 GID/token 后按当前选择的 E-Hentai 或 ExHentai 源重建地址并直接打开详情，非法地址通过状态文字和 InfoBar 明确提示。
- 评论解析新增站内画廊引用：评论正文中的 EH/EX 画廊链接渲染为应用内按钮，可连续跳转前作、续作或其他关联画廊，返回时逐级恢复上一详情；外站及非画廊链接不会获得跳转能力。
- RSViewer schema 升级到 v19，为下载评论与同步评论新增 `gallery_links_json`，保存经严格校验的 GID、token 和链接文字，确保本地同步详情及应用重启后仍可跳转。
- RSViewer schema 升级到 v18：v14 在自有 SQLite 中补齐 EhViewer v7 的 11 张兼容表，v15 把旧回收站记录的还原目标统一迁移到当前自有数据库，v16 补齐 `IDX_Black_List_BADGAYNAME` 显式索引，v17 新增原图画廊基础图回退标记，v18 新增逐页 `original` / `base` 类型 checkpoint。
- 原图下载改为逐页混合策略：每页有 `fullimg` 就下载原图，仅该页没有时回退基础图；直接下载、本地 `original/` 暂存、断点续传、原图替换和未来版本更新均保留页级类型。混合画廊保持彩色边框与左上角紫色圆点，详情右上角显示金色 `X ORIGINAL` 和紫色 `Y BASE`，全原图画廊显示彩色 `ORIGINAL` 标签。
- 新增 `scripts/import_ehviewer_database.py`，以只读 SQLite 快照把已有 `eh.db` 合并或 `--replace` 导入 `app/data/rsviewer.db`，并同步既有收藏与历史到 RSViewer 关系表；导入过程不修改源文件。
- 设置页原 EhViewer 数据库路径选择改为“导出 EhViewer 数据库”：后台从自有库新建完整 `user_version=7` 兼容数据库，合并当前收藏/历史并通过完整性校验后原子写入用户选择的位置。
- 本地资源卡片、已下载的在线资源卡片及本地画廊详情新增“在资源管理器中打开”；本地复选模式下始终只打开实际右键的那一本，不扩大到当前复选集合。
- 更新管理任务卡新增“删除”操作：运行中的任务先安全取消，随后只移除 RSViewer 任务记录，保留画廊文件与目录 checkpoint，便于处理失效测试任务而不破坏恢复现场。
- 本地资源卡片新增复选批量“移入回收站”，漫画导航新增响应式封面卡片回收站；支持全选、工具栏和右键还原/彻底删除。软删除仅移除外部登记并保留图片，还原无需确认，永久删除必须确认后才清理目录与自有关联。
- RSViewer schema 升级到 v13：v12 `gallery_trash` 保存外部三张业务表的精确快照及 `moving/trashed/restoring/deleting/failed` 状态，v13 记录原 `eh.db` 和漫画根目录，配置切换后仍能原位还原；启动恢复、整理页排除和多窗口即时刷新均接入该状态。
- 整理页由横向任务行改为与本地资源一致的响应式封面卡片网格，保留常驻复选、全选和右键批量同步/回收站删除。
- 底部导航在“设置”正上方新增“新窗口”动作，可在同一进程打开完整 RSViewer 窗口；多个窗口共享下载、更新、原图文件操作和整理线程池，并同步资源、收藏、历史、阅读进度、下载、更新、整理及数据源状态。跨窗口暂停/删除会交给任务实际所属窗口，同一画廊不会重复启动；下载全局最多 3 项、更新全局最多 1 项。
- 漫画导航新增“整理”页：右上角后台扫描本地存在但 `eh.db` 未登记的资源目录，支持复选、全选及右键批量同步或删除；同步保持原目录并补写外部既有业务表和 RSViewer 自有记录，删除经确认后移入 Windows 回收站。
- 画廊版本更新改为全局单任务执行，其余任务持久化为等待更新并在当前任务结束后按入队顺序自动继续；画廊下载并发配置与运行时上限收紧为最多 3 项。
- 本地与在线画廊详情新增“下载原图”及独立进度条：严格读取当前单图页的同站 `fullimg` 链接；新在线画廊直接建立原图资源，已有本地画廊先断点下载到 `original/`，完成后可在标准/原图预览及阅读来源间切换。
- 新增可恢复的“原图替换”和显式“删除压缩图”：替换先把基础图归档到 `history/del/`，再逐页提升原图；基础备份只在用户二次确认后删除，下载、替换和清理均保存阶段状态。
- RSViewer schema 升级到 v11：下载任务新增基础/直接原图/本地转原图模式，并新增 `gallery_original_states` 保存原图资源、断点与文件操作阶段；原图画廊更新版本时继续下载原图。
- 本地资源卡片新增下载状态提示：完整原图画廊显示彩色渐变边框，正在原图下载或待替换的画廊显示暗黄色边框，未完成基础下载显示右上角黄点；下载管理页同步显示三种任务类型。

- 本地画廊同步检测到新版本后新增“更新到最新”：使用原子 `new.json`、`new.ehviewer`、页 token 临时文件名和 `.part` 下载实现可重入的六阶段恢复；旧版删除页归档到 `history/removed/`，完成后事务迁移外部 GID 与自有用户关联。
- 漫画模式新增独立“更新管理”页，集中显示 checkpoint、图片进度、速度和失败原因，支持开始、暂停和跨重启继续。未完成原画廊会先补齐；更新中禁用阅读、同步和补页，播放列表自动跳过。
- RSViewer schema 升级到 v10，新增 `gallery_update_tasks` 作为更新管理的快速任务索引；目录内 `new.json` 仍是文件恢复 checkpoint，每次恢复都以实际文件校验为准。

- 本地未完成画廊支持按需补页：详情按 `.ehviewer` 总页数显示完整预览格，缺失页使用在线缩略图；点击缺失页后仅下载该页到原目录再进入阅读，纯在线阅读仍不落盘。阅读器工具栏同步显示画廊下载页数进度与实时速度。
- 下载任务卡片新增实时速度显示，第一张图片完成前显示“测速中”，之后按单页实际字节与请求耗时进行平滑计算；页面标题区同时显示所有活动画廊的汇总速度，暂停、失败或完成后即时清除。
- “正在下载”页面新增“全部开始”和“全部暂停”按钮，可一次启动所有未活动任务，或暂停全部正在获取详情、准备 sidecar 和下载图片的任务；按钮会根据任务状态自动启用。
- 在线资源 Card 与 Extended 卡片新增右键“下载”：无需先打开详情页，任务会立即进入“正在下载”，随后在后台获取完整元数据、评论和页面标识并沿用既有断点下载流程。
- 在线设置新增“默认下载分类”，选项来自当前 EhViewer `DOWNLOAD_LABELS`，默认为未分类；新建下载条目写入所选 `LABEL`，任务记录同时保存目标分类以供中断恢复，已有画廊继续下载时保留原分类。
- 本地/在线共享画廊详情页新增“查看详细”折叠入口：播放列表、归类、页数、阅读进度、已下载/完整状态、文件大小和可见性默认隐藏，展开后集中展示并保持文字可选中复制。
- 本地资源复选模式新增跨分页“全选/取消全选”，范围遵循当前分类、播放列表、归类及搜索筛选；右键菜单新增“同步在线信息”，可按选中集合最多并行 2 项批量刷新源站标签、评论和版本信息，整批完成后统一刷新本地资源。
- 本地画廊详情新增“同步信息”：直接请求当前 GID/token 对应的 EH/EX 详情 HTML，在后台刷新标题、发布者、评分、标签等 `eh.db` 既有字段，并将评论和额外元数据保存到 RSViewer 自有数据库；同步不改变原下载状态，也不下载页面图片。
- 详情解析新增画廊版本链检测：读取详情页 `#gnd` 中同站合法更新版本链接；本地同步后若当前画廊是旧父画廊，详情页显示醒目的旧版本状态和检测到的更新版本数量。
- RSViewer schema 升级到 v9，新增与下载任务状态相互独立的画廊同步记录和评论快照；普通 EhViewer 本地画廊无需创建下载任务也能跨重启恢复同步信息。
- 漫画模式新增独立“正在下载”页面：未完成任务集中显示页数进度，可开始、继续、暂停或删除任务记录（删除不移除已下载文件）；完成任务自动移出页面，设置中可即时配置 1–3 个画廊并发下载。
- 本地画廊详情新增源站补齐入口：后台读取 `VERSION2` `.ehviewer` 的画廊 token、总页数和逐页 token，即使外部 `DOWNLOADS.STATE` 已标记完成，也会按磁盘实际有效页判断完整性并只下载缺失或损坏页面。

### Changed

- 在线下载预登记、外部注册、暂停、失败和完成阶段改为按单个 GID 精确读取并增量更新本地卡片；下载完成不再整库扫描、清空搜索或切换到“显示全部”，当前分类、播放列表、归类、页码和滚动位置保持不变，多窗口通过单条资源事件即时同步。
- 本地封面缩放结果改为在封面或控件尺寸变化时缓存重算，滚动重绘直接使用已裁切位图，避免每一帧对可见卡片重复执行平滑缩放。
- 本地列表、分类、在线下载、版本更新、整理、原图操作和回收站全部改用单一 RSViewer 自有 SQLite；运行时不再读取或修改外部 `eh.db`，旧配置中的数据库路径键被忽略。
- `EhViewerDownloadRepository` 继续维护兼容字段和目录规则，但所有实例都接收 `UserLibraryRepository.database_path`；兼容表建表只由自有 schema migration 负责。
- 基础下载、原图下载、单页补图和画廊版本更新的速度改为在图片响应流中定期更新，不再等待整张大图下载完成。
- 本地画廊完成原图替换或版本更新迁移后同步刷新外部 `DOWNLOADS.TIME`，使本地资源“最新优先”排序体现内容更新时间。
- 在线画廊详情新增后台下载、暂停与断点继续：先持久化画廊元数据和评论，再收集全部 `/s/{page-token}/{gid}-{page}` 标识并逐页下载 `#img` 站点展示图；重试耗尽或应用中断后保留状态，继续时校验并跳过已有有效页面，只补缺失或损坏文件。
- 下载目录完全复用设置中的 EhViewer 漫画根目录和目录命名规则，生成 `VERSION2` `.ehviewer`、`.thumb` 与八位页码图片；目标 `eh.db` 仅事务更新既有 `DOWNLOADS`、`DOWNLOAD_DIRNAME`、`Gallery_Tags`，保留原分类/归档字段且不执行 DDL。
- RSViewer schema 升级到 v8，新增唯一 GID 的在线下载任务与评论快照；保存额外元数据、已完成页数、暂停/失败原因和更新时间，应用启动时把上次未结束任务恢复为可继续状态。
- 在线画廊详情新增按站点原生 20 张分页的缩略预览；切换预览页只在缓存未命中时直接请求画廊 `?p=N` HTML，点击缩略图可从精确页码进入共享漫画阅读器。
- 漫画阅读器新增在线来源：后台按需请求 `/s/.../{gid}-{page}` 单图 HTML 和其中 `#img` 展示图，沿用本地阅读器的翻页、跳页、缩放、长图、全屏、方向键、自动翻页及相邻页预读，不调用 API 或原图下载接口。
- 新增最近 20 个在线画廊的线程安全 LRU 内存缓存，复用详情、封面、已访问预览分页和缩略图；阅读图片每画廊最多保留 5 页并受 128 MiB 总预算约束，损坏数据会丢弃重取。
- 在线资源卡片现在在应用内打开画廊详情，不再直接跳转系统浏览器；详情沿用本地漫画详情页的标题、封面、元数据和分组标签布局，并按来源正确返回在线列表。
- 在线画廊详情与评论通过当前 provider 的同一 `requests.Session` 直接 GET 站点 `/g/{gid}/{token}/` HTML 页面并在后台解析，没有使用官方或第三方 API；评论区只读展示作者、时间、上传者标记、评分和可复制正文，无评论与请求失败均有明确状态。
- 本地漫画详情页的标题、英文标题、元数据和标签文字支持鼠标/键盘选择，并可通过 `Ctrl+C` 复制。
- 在线资源 Card 与 Extended 卡片新增本地下载标记：主窗口复用本地资源首次加载的 GID 集合，命中画廊在左上角显示绿色下载图标；未下载画廊不显示标记，本地数据源重载后当前在线页面即时同步。
- RSViewer schema 升级到 v7，新增共享 `search_history`：本地资源、收藏、本地历史和在线资源确认搜索时保存查询，重复内容移到最近位置，跨重启恢复；历史硬上限 20 条，设置页可选择保留 5/10/15/20 条并即时裁剪。
- 搜索候选将匹配的历史查询优先排列在标签补全之前；输入框为空时聚焦也可查看最近历史。标签候选由括号格式改为两行显示，例如第一行 `other：full color`、第二行“全彩”，选中后仍补为 `o:"full color"`。
- 新增 `scripts/import_eh_tags.py`：解析 `Database/database` 中按命名空间拆分的 Markdown 表格，将英文原始标签、中文名称、描述、外部链接、命名空间缩写和别名以可重复的完整快照导入 RSViewer 自有数据库；解析与校验在事务替换前完成，不修改外部 EhViewer 数据库。
- RSViewer schema 升级到 v6，新增 `eh_tag_namespaces` 与 `eh_tags`；主程序启动时一次加载已导入标签并建立内存字符/双字检索表，本地资源、收藏、本地历史和在线资源共享中文译名及英文片段联想。
- 本地与在线搜索框新增光标感知的多标签下拉补全：如“全彩”显示 `other:full color(全彩)` 并补为 `o:"full color"`；空格分隔后可继续补全“汉语”等条件，键盘方向键、Enter/Tab 和鼠标均可选择，已有条件不会被覆盖。本地搜索同时支持解析 EH 命名空间缩写和引号中的多词标签。
- 在线资源新增“卡片 / Extended”视图切换：卡片模式按封面、类型/评分、悬停滚动长标题、发布时间/上传者/页数分区；Extended 模式以横向信息行展示完整标题、元数据和自动换行标签。切换即时保存为用户默认视图，并在后台通过原列表页的 `inline_set=dm_l/dm_e` 同步站点账户默认模式、重取当前页完整字段。
- 在线资源顶部新增“刷新”按钮，可绕过内存页缓存重新请求当前站点、关键词和游标对应的页面。
- E-Hentai 与 ExHentai 新增互相隔离的内存页面容器，分别保存搜索词、当前页、翻页历史、滚动位置和最近 64 页结果；切站自动恢复原状态，空容器才请求首页。
- 在线封面新增按站点分目录的磁盘缓存，默认 7 天过期；设置页可配置 1 小时至 30 天，过期或无法解码的文件会惰性删除并重新下载。
- 在线设置新增封面并发请求数，支持 1/2/4/6/8/12 个任务并即时更新专用线程池。
- 将用户提供的 `eh_tool_refactored.py` 接入在线资源：继续使用其 `requests.Session`、EH/EX HTML 画廊列表页、`BeautifulSoup`/`lxml` 解析、五种列表模式以及页面自带的上一页/下一页 URL；关键词通过同一列表页的 `f_search` 参数提交，没有引入其他站点 API。
- 在线资源卡片补充上传者和评分展示，并为页面、滚动视口、卡片及封面增加独立的浅色/深色主题 QSS。
- 新增可配置的“展开漫画搜索栏”和“展开/收起漫画标签栏”全局快捷键；两者会切换到本地资源后执行，沿用点击捕获一次按键的设置交互并支持组合键。
- 界面设置新增“鼠标悬停展开搜索栏”：移入本地资源搜索按钮时不抢焦点地展开，搜索词为空且离开按钮/输入区后自动收起，有内容则持续显示；关闭配置后即时停止悬停响应。
- 漫画右键菜单新增“搜索相似画廊”：后台分析英语标题和原标题，剥离开头社团/活动括号、末尾语言与数字版标记，以及 `Ch./Chapter/Vol./Part`、第 N 话/章/卷、前后篇等章节信息，在完整本地库中查找同作品章节和重复画廊；长标题支持保守模糊匹配，短标题仅使用高置信规则以降低误报。修改搜索词或切换标签即可退出相似结果。
- 播放列表画廊的漫画右键菜单新增“从当前播放列表移除”，支持单本和复选批量移除；只删除当前播放列表成员关系，不影响漫画文件或其他播放列表。
- 漫画阅读器新增 GIF 动图播放；使用 `GIF87a`/`GIF89a` 文件头识别真实格式，因此页面即使使用 `.jpg` 等错误后缀也能播放。相邻页预载只缓存首帧，切到当前页后才启动动画，并兼容现有缩放、全屏与翻页操作。
- 在线资源新增 `EhOnlineProvider` 扩展契约、统一查询/分页领域模型、后台搜索/封面 Worker 与通用结果页；组合入口现已使用用户提供的 `eh_tool_refactored.py` 实现列表获取、结果转换和封面加载。
- 设置页新增 EH Token/Cookie、默认站点、请求超时和代理配置；代理支持跟随系统、直连和手动 HTTP(S)，完整 Cookie 以密码输入框显示且仅保存到被忽略的本机配置。
- `EhOnlineSettings` 提供站点基址、Cookie 规范化、代理映射和超时校验，凭据不出现在对象 `repr`；保留空 provider 作为兼容实现，并新增具体爬虫适配、配置和界面 Worker 测试。

- 漫画卡片右键“添加到分类”子菜单新增“移至未分类”，支持单项和复选批量解除分类，同时保留分类标签本身。
- 增加本地漫画收藏：资源卡片右键可单项或复选批量“添加到收藏/取消收藏”，收藏页使用与本地资源相同的分页封面视图。
- 增加本地浏览历史：打开漫画详情、开始阅读或播放列表切换漫画时写入历史，历史页按最近访问顺序展示；同页预留独立“在线历史”入口。
- RSViewer schema 升级到 v5，新增 `manga_favorites` 与 `manga_browsing_history`，收藏和历史均不写入外部 EhViewer 数据库。
- 分类、播放列表和归类树增加右键删除操作及 Fluent 确认框；“未分类”保留为不可删除的系统入口。
- 漫画详情新增独立标签卡片，按作者、社团、Cosplayer、原作、角色、语言、女性、男性、混合、杂项等命名空间分组，并使用随主题切换的 `el-tag` 风格胶囊控件展示。
- 本地资源标签栏拆分为互斥的“分类”“播放列表”“归类”三种视图，每种视图均提供独立加号；标签栏可拖动调宽，最大不超过页面宽度的 30%。
- 分类默认进入并记忆上次选择，默认项为“未分类”；“显示全部漫画”从标签树移到标签栏顶部的独立按钮。
- RSViewer schema 升级到 v4：原复数标签作为多对多播放列表继续兼容，新增持久播放顺序、上次播放漫画，以及支持任意父子层级的多对多归类表。
- 播放列表操作栏增加“播放”“继续上一次”和“编排顺序”；阅读到一本漫画末页后继续翻页会按编排顺序打开下一本，详情页进入阅读时也保留播放列表上下文。
- 资源右键菜单提供“选择分类”“选择播放列表”“选择归类”三个固定入口，打开可搜索、可滚动的独立选择窗口；分类单选，播放列表和树状归类多选并支持批量半选状态。
- 本地资源操作栏增加“复选”和选中数量提示；复选模式下点击卡片选择项目，右键可对全部选中漫画统一执行三类标签操作。
- 本地资源增加添加时间排序选择，默认降序显示最后添加的漫画，并可切换为升序。
- 增加 RSViewer 自有 `manga_reading_progress` 表和 `PRAGMA user_version=2` 可重复迁移，页码变化经单线程后台队列防抖保存。
- 增加 `.ehviewer` 第二行十六进制零基页索引兼容：如 `0000008f` 显示为第 144 页；仅在自有记录不存在时导入，两边都有时自有记录优先。
- 本地资源的网格/列表卡片与漫画详情显示阅读页码；再次打开默认恢复到自有进度。
- 详情页页面预览支持点击任意缩略图并从对应页码直接进入阅读模式。
- 增加阅读页内设置按钮和非模态设置面板；背景色、翻页方向、图片载入大小、滚动快捷键、自动翻页开关及间隔与全局设置即时双向同步。
- 增加四向翻页规则：从左向右使用 `←` 下一页，从右向左使用 `→`，从上向下使用 `↑`，从下向上使用 `↓`；相反方向键返回上一页。
- 增加“适应宽度（长图）”模式和可配置的向前滚动快捷键，按键滚动约一屏并在图片底部进入下一页。
- 增加自动翻页工具栏开关，可配置 2 至 30 秒间隔；离开阅读页或到达末页后停止计时任务。
- 增加漫画阅读器，支持窗口内阅读、全屏切换、键盘和按钮翻页、页码跳转、适应窗口、原始大小、缩放与拖动。
- 阅读器在后台优先解码当前页，并预读后两页和前一页，避免翻页时同步访问磁盘或 NAS。
- 增加可配置的 EhViewer 外部数据库和本地漫画根目录，配置变更后自动重载。
- 增加 EhViewer 数据源、RSViewer 独立用户数据库、本地漫画封面分页、搜索、分类筛选和详情页按需预览。
- 增加针对外部数据源只读性、惰性页面加载、封面回退、自然排序、快捷键捕获和缺失配置的自动化测试。
- 增加点击后捕获一次键盘输入的快捷键设置，支持 `Ctrl+S` 等组合键和单键，按下后立即确认，`Esc` 取消。

- 建立 RSViewer 桌面应用入口、Fluent 主窗口和设置界面。
- 增加左侧“漫画”导航树，包含本地资源、收藏、在线资源和历史记录入口。
- 增加独立的“视频”导航入口。
- 增加媒体功能占位页，为后续服务和视图实现提供稳定路由。
- 增加媒体目录配置，面向本地目录、Windows 映射盘和 UNC/NAS 路径。
- 增加浅色与深色设置页 QSS，并注册到 Fluent 样式管理器。
- 增加 `README.md`、`requirements.txt`、`.gitignore` 和项目维护指南 `AGENTS.md`。

### Changed

- 在线资源移除不存在实际含义的数字页码和本地翻页历史；E-Hentai / ExHentai 的上一页、下一页按钮现在分别且只依据当前响应的 `prev_url` / `next_url` 启用并直接请求对应游标，日期 Seek 结果不再被误标为第一页。
- 本地资源页标题改为当前筛选标签：分类和播放列表显示标签名，树状归类显示 `父级/子级/...` 完整路径，“显示全部漫画”仍显示“本地资源”。
- 本地资源搜索按钮改为可锁定的悬停交互：悬停仍临时展开，点击后常驻并聚焦，再点击解除常驻、恢复移出自动收起；快捷键和相似画廊搜索继续显式常驻。
- 本地详情原“检查并补齐”改名为“基础下载”；详情操作区改为可换行布局，双下载进度和文件操作按钮在窄窗口下不会越界。

- 详情页下载进度条移动到下载按钮下方；排队、下载、暂停或失败状态直接在按钮文字中显示“已完成页数 / 总页数”，下载中点击同一按钮仍可暂停。
- 左侧导航由“漫画”树状父路由改为底部模式切换：在“设置”上方新增“漫画”“视频”按钮，漫画模式在顶部显示本地资源、收藏、在线资源和历史记录，视频模式当前只显示资源入口；切换时复用常驻页面并进入对应默认页。
- 在线资源的“卡片 / Extended”分段选项改为单个布局图标按钮；点击在两种布局间切换，图标随当前状态更新并通过工具提示说明目标布局。
- 在线默认卡片移除类型/评分、标题、发布时间、上传者和页数周围的独立边框与底色，改为按既定位置直接显示文本；上传者不再添加“上传者：”前缀，并与页数零间距紧凑堆叠。类型保留独立色块并按 EH 的 ct1–cta 官方分类渐变配色，Extended 视图同步使用相同类别样式。
- 在线画廊领域模型将评分收敛为可空的 `0–5` 浮点数，并保留源页面显示模式；Minimal/Minimal+ 页面没有标签节点时，Extended 视图显示明确的缺省提示而不是解析错误。
- 在线封面由单个 Worker 顺序下载改为每张封面一个任务，通过独立 `QThreadPool` 并发执行；任务优先读取本地缓存，未命中才复用当前 provider 会话请求并原子落盘。
- `create_eh_online_provider()` 改为创建 `RefactoredEhOnlineProvider`；首次搜索调用原爬虫 `getMain()`，后续翻页调用原页面结果生成的 URL，远程封面复用同一爬虫会话。
- 运行依赖增加 `requests`、`beautifulsoup4` 和 `lxml`，分别用于复用用户脚本的 HTTP 会话、HTML 解析和既定 `lxml` 解析器。
- 标签栏分隔拖动柄改为与侧边导航栏一致的 Fluent 细线：保留 7 像素命中区域，只绘制 1 像素主题色线条，悬停或拖动时提高透明度。
- 收藏页和历史页复用本地资源首次加载的漫画元数据，不会为新增页面重复查询外部库或扫描漫画根目录。
- 删除分类时先把关联漫画移到“未分类”并删除外部 `DOWNLOAD_LABELS` 既有数据；删除播放列表会清理成员和顺序，删除归类节点会级联清理子树及关联，所有操作均不改变外部数据库 schema。
- 本地资源页左侧标签栏改为默认收起，通过工具栏“标签”按钮展开或再次隐藏；现有分类、播放列表和归类选择状态保持不变。
- 详情标签展示优先使用带命名空间的数据并去除搜索用裸标签副本；未知命名空间和真正无命名空间的标签归入独立分组，标签较多时在双栏内自动换行。
- 原“分类标签”改名为“分类”，原“复数标签”改名为“播放列表”；播放列表不再使用复选筛选，选中一个播放列表后按其自定义顺序展示。
- 归类父节点筛选会同时包含全部后代节点的漫画；分类、播放列表和归类任一时刻只展示其中一种筛选结果。
- 分类标签写入策略由 RSViewer 自有覆盖改为用户显式操作时后台事务更新目标 `eh.db`；浏览和其他数据读取仍使用只读连接，外部库不新增任何 RSViewer 表。
- 漫画详情的页面预览改为固定每页 40 张，只创建和解码当前预览页；2000 页漫画显示为 50 个预览分页，点击仍使用真实页码进入阅读。
- 本地漫画数据保留 EhViewer `DOWNLOADS.TIME`，列表筛选后统一按添加时间和 GID 稳定排序，不再按标题覆盖数据库顺序。
- 全屏阅读改为边缘触发的沉浸式控制：默认隐藏全部操作区，鼠标到最上沿时只显示标题/操作栏，到最下沿时只显示翻页栏，离开后收起；键盘翻页和普通鼠标活动不会唤出控制栏。页码脱离操作栏，以低透明度无背景文字持续显示。
- 阅读器页码从底部小号总页数提示移到顶部工具栏下方居中位置，改为醒目的“第 N / 总页数 页”；底部仅保留翻页、页码跳转和缩放状态。
- `.ehviewer` 进度采用当前页/后续预载页及详情页按需读取，避免启动时逐本访问两万多个 NAS sidecar；文件始终只读，后续进度仅写 RSViewer 数据库。
- 阅读器工具栏的“适应窗口”和“原始大小”操作改为更新全局阅读配置，页内面板和全局设置页会同步显示新值。
- 将大型漫画库加载改为两级惰性策略：列表只枚举一次漫画根目录并读取 SQLite 元数据，单本页面在打开详情时才扫描，避免启动时遍历数百万图片。
- 移除无内容的漫画展示首页；启动仍直接显示本地漫画，同时恢复独立“漫画”父路由和“本地资源”子项，为后续更换漫画首页保留结构。
- 本地漫画页后台预读当前页及后续三页的缩放封面，翻页不再同步访问和解码 NAS 图片。
- 配置文件改为基于项目根目录的稳定绝对路径，不再依赖启动时的当前工作目录。
- SQLite 连接在操作结束后显式关闭，允许运行中切换外部数据库并避免 Windows 文件句柄残留。

- 将本地 `testData/db/eh.db` 明确为默认只读的外部漫画数据源；仅显式分类操作可更新 `DOWNLOADS.LABEL`，RSViewer 自有表仍必须使用另一份独立 SQLite 数据库。
- 忽略本地测试数据目录和本地 `lib/` Python 环境，避免提交用户数据库、漫画样例和环境文件。
- 将项目定位从 PyQt-Fluent-Widgets 示例骨架明确为个人非商业用途的漫画、图片与视频管理工具。
- 将旧音乐/下载目录配置替换为 `Library/Folders` 媒体库配置。
- 重构 `main.py`，使用显式 `main()` 入口并保留翻译器生命周期。
- 主窗口标题和图标改为 RSViewer 自身标识，不再依赖 Gallery 资源。
- 精简自定义样式枚举，只保留项目实际使用的设置页样式。

### Fixed

- 修复并发页面下载结束或重试时，内部汇总速度短暂归零，任务卡片会从速度数值反复跳回“测速中”的问题；速度采样放宽为约 1 秒一次，活动任务取得首个有效值后会保留最近速度，直到暂停、失败或完成时再清除。
- 修正画廊并发与图片并发被混为一个限制的问题：设置页拆分为 1–3 个“画廊下载并发数”和所有活动画廊共用的 1–6 个“图片下载线程数”。画廊超出上限时保持排队；单个活动画廊可占用多条页面线程，多个活动画廊通过进程级公平调度器共享总线程上限，页面文件仍逐页原子写入并串行汇总断点、进度与状态。
- 修复在线资源右键下载偶发无响应：SQLite 任务/兼容表写入、目录创建、封面落盘和本地条目读取不再阻塞 GUI 线程，而是进入多窗口共享的串行预登记池；点击后立即标绿并占用任务 GID，预登记完成后再获取详情和进入下载队列。
- 修复在线画廊点击下载后，任务因等待详情请求或下载线程槽而尚未建立本地条目，导致资源页不显示绿色下载图标、用户无法判断是否已加入下载的问题；列表右键和详情下载现在都会立即持久化已有元数据与兼容索引、创建目录、保存已取得的有效 `.thumb`，并在多窗口中同步绿色标记，完整详情、`.ehviewer` 和页面图片仍由后续 Worker 补齐。
- 修复版本更新完成后的末尾进度信号会把任务重新写成“正在更新”，导致完成卡片当场及重启后仍留在更新管理的问题；完成 checkpoint 现在优先于活动状态，启动时也会自动修正既有的异常记录。
- 修复大型画廊存在重复 page token 时，旧图片标识与重排按单值映射而遗漏其中一页，并因 checkpoint 已前进而无法在继续任务时自行修复的问题；标识、重排和验证现在按 token 出现次数配对，阶段 1–4 继续任务时会幂等补齐旧页标识并修复实际文件。
- 修复原图流式连接或响应体长时间无进展时，任务会按 60 秒请求超时连续等待、界面仍显示上一页速度而看似永久卡死的问题；图片流式请求连续 15 秒无进展会主动超时并进入既有三次重试，重试时清除陈旧速度，手动暂停不再是唯一恢复方式。

- 修复在线资源 Card/Extended 卡片真实鼠标右键释放时仍触发 `CardWidget.clicked`、直接进入详情而无法使用“下载”上下文菜单的问题；在线卡片现在仅允许左键打开详情，右键稳定弹出下载菜单并继续使用统一下载任务链路。
- 修复未完成画廊用紧凑 `page_paths` 把中间缺页后的图片前移到错误页码，以及每落盘一张图片都会重绘详情元数据并重建整页预览的问题；本地页现在按八位数字文件名映射，逐页事件只增量替换目标预览格和阅读缓存。
- 修复在线画廊开始下载并已写入外部 `eh.db` 与目录后，本地未分类列表和在线卡片的本地标记仍不刷新、必须重启或等待最终完成才可见的问题；Worker 现在分别上报本地条目注册、`.ehviewer` 就绪和逐页落盘阶段，主窗口同步刷新本地库、在线标记及已打开的本地详情。普通后台刷新同时保留当前分类/播放列表/归类、显示全部、搜索、页码、有效复选项和封面内存缓存；早期暂停或初始化失败也会正确持久化为 paused/failed，不再残留为 queued。
- 修复首次启动画廊下载后在外部 `eh.db`、下载目录或 `.ehviewer` 尚未创建时退出，重启后继续任务只会反复等待本地资源出现并失败的问题；恢复流程现在从 RSViewer 自有任务记录重建受站点约束的 GID/token 详情请求，重新获取元数据与评论，再按标准顺序初始化目录、sidecar 并继续下载。
- 修复应用重启后继续未完成下载时，新增图片虽已写入磁盘，但已打开的本地详情预览和阅读器仍持有旧 `page_paths`、只能看到续传前页面的问题；下载 Worker 现在逐页上报落盘路径，详情预览与阅读器会增量扩展可见页面。
- 修复本地画廊同步信息后返回上一级会丢失已选分类筛选、变成显示全部的问题；同步完成现在只刷新元数据并保留当前列表筛选，下载完成后的新画廊定位行为保持不变。
- 修复存在活动画廊下载时退出缓慢：关闭窗口会立即隐藏界面并持久化暂停状态，清除未启动任务，通知独立 provider 关闭正在流式读取的响应，下载线程池收尾等待由 3 秒缩短到最多 1 秒。
- 修复启动阶段 Splash 在主窗口完成首次尺寸事件前仍使用默认小尺寸，导致中央短暂闪现带标题栏的白色小窗；Splash 创建后现在立即同步主窗口尺寸。
- 关闭 Fluent 主 stacked widget 默认的 300ms 整页位移动画，避免最大化或全屏窗口切换页面时反复重绘大面积透明背景与媒体卡片而明显掉帧；页面仍保持常驻，不会在切换时重新创建。
- 修复部分下载画廊从本地资源进入后把现有文件数误当成总页数的问题；详情现在显示 `.ehviewer` 声明的真实总页数和“已下载 / 总页数”，阅读器仍只开放磁盘上实际存在的页面。
- 修复在线画廊下载完成后本地资源、共享收藏/历史元数据和在线下载标记不立即更新的问题；完成刷新现在携带目标 GID，忽略过期的后台加载结果，清除会遮住新画廊的旧搜索条件并切到“显示全部”后定位其分页，无需重启应用。下载记录中的上传者、发布时间、评分、语言、文件大小等扩展元数据也会批量合并到本地条目并显示在详情页。
- 修复在线画廊预览忽略 EH/EX 缩略图精灵图的 CSS 尺寸与背景偏移，导致同一批页面全部显示为第一张预览的问题；现在解析每项裁剪区域，后台从共享精灵图裁出独立缩略图并缓存裁剪结果。
- 修复搜索补全菜单展开后，点击页面其他位置会短暂收起又立即弹出、持续抢占鼠标而导致其余按钮无法操作的问题；菜单关闭或搜索框失焦时现在会取消待显示任务，并忽略弹层自身的焦点归还事件。
- 修复 Extended 切回卡片布局时，内存复用的封面会在卡片完成布局前按 `QLabel` 临时宽度缩小，之后只能通过刷新恢复的问题；封面控件现在保留源图，并在几何尺寸变化时自动按比例重新生成 pixmap。
- 修复在线资源切换卡片/Extended 布局时销毁并重建卡片会让已显示封面重新进入加载状态的问题；当前页有效封面现在以内存数据跨本地重建和远端模式同步后的二次重建直接复用，不再创建重复封面任务。
- 修复布局切换或页面恢复安排的零延时滚动回调可能在在线资源界面销毁后访问失效 `ScrollArea` 的问题；恢复任务现在由界面所属的单次计时器管理。
- 修复 Windows 使用单一系统 HTTP 代理端点时，“跟随系统”把 HTTPS 目标误配置为 TLS 代理连接而无法访问、只有重复填写手动代理才生效的问题；现在会把标准库展开出的同端点 HTTP/HTTPS 映射规范化为 `requests` 可用的 HTTP CONNECT 代理，同时保留不同端点的分协议配置。
- 修复 EH 标签联想错误地绕过 `SearchLineEdit` 内置补全机制、同时弹出 Qt 原生白色大面板，导致候选文字不可见且深色主题失配的问题；现在只使用 Fluent 圆角补全菜单，候选内容正常显示，最多展示 8 行并可滚动，浅色/深色主题即时同步。
- 修复在线评分把 EH 星级精灵图的 CSS `background-position`/`opacity` 当作分数展示的问题；现在优先读取数值文本，否则按半星精灵位置反解为真实评分，并在 provider 边界拒绝非数值和越界结果。
- 修复在线资源页面未注册项目主题样式、滚动区域和结果卡片沿用 Qt 默认底色，导致深色主题下页面风格与主体不一致的问题。
- 修复标签数量较多时右键级联子菜单向窗口错误方向展开、位置偏移而无法点击，且长列表超出屏幕显示不全的问题；标签选择不再依赖级联悬浮菜单。

- 修复标签栏筛选完成后直接收起时，`QSplitter` 内容区虽然扩宽但封面网格仍沿用旧列宽、导致右侧大片空白，只有调整窗口大小才恢复的问题；展开、收起和拖动分隔条现在都会在几何更新后主动重排卡片。
- 修复播放列表“编排顺序”缺少可用拖拽交互、保存目标可能随当前选择变化且没有成功反馈的问题；现在支持拖拽、上下移动、置顶/置底，并固定保存到打开对话框时的播放列表。
- 修复播放列表连续阅读只能从上一本末页进入下一本、无法从下一本首页返回的问题；现在首页执行“上一页”会打开上一本的最后一页。
- 修复未开启复选时右键卡片会沿 `CardWidget.clicked` 路径进入漫画详情、导致单项右键菜单看似不可用的问题；网格和列表现在仅响应左键进入详情，右键始终保留给操作菜单。
- 修复数千页漫画在刷新详情预览时一次创建、布局和解码全部页面，导致界面卡顿数分钟的问题。
- 修复阅读设置弹窗在深色主题下仍使用 Qt 默认白色背景、导致灰色文字对比度异常的问题；弹窗现已注册 light/dark QSS 并随主题即时刷新。
- 修复点击漫画图片后焦点进入 `QGraphicsView`，导致左右方向键被视图吞掉而无法翻页的问题。
- 修复约 2 万画廊、400 万图片的数据集在进入本地资源时需要数分钟的问题；本机 21,389 条真实数据列表读取约 0.42 秒，完整首屏约 1.33 秒。
- 修复快速关闭详情页时后台预览任务向已销毁 Qt 对象发送信号的问题。
- 修复缺少或损坏 `.thumb` 时错误显示“无封面”的问题，自动使用自然排序后的第一张有效页面。

- 修复切换深色主题时设置内容区仍为浅色、标题颜色错误以及出现明显 Qt 默认边框的问题。
- 修复媒体目录设置卡片将父组件误传为目录参数的问题。

### Security

- 移除 `eh_tool_refactored.py` 中原有的硬编码 Cookie 与本地代理地址，改为仅从运行时设置注入；翻页只接受当前 EH/EX 站点的 HTTPS URL，封面加载限制在 EH/EX 及 `ehgt.org` 图片域名。

### Removed

- 删除 `examples/` 下的 PyQt-Fluent-Widgets Gallery 和控件演示副本。
- 删除旧 Gallery 文档、安装脚本、示例图片/音频、翻译、图标和无用公共模块。
- 删除约 26 万行的旧 Qt Gallery 生成资源 `app/common/resource.py`。
- 删除音乐播放器模板中的 `musicFolders`、`downloadFolder` 和第三方示例常量。

### Validation

- `.venv\Scripts\python.exe -m unittest discover -s tests -v`：212 项测试全部通过；新增服务器响应游标导航、本地筛选标题、画廊任务排队、共享页面线程和速度显示稳定性测试，覆盖日期定位双向翻页、在线列表无数字页码、分类/播放列表/多级归类标题、画廊并发设为 1 时其余任务排队、单画廊占用多条页面线程、并发原图回退 checkpoint、并发暂停收束及零速度不覆盖最近有效值。`compileall`、Qt 离屏界面测试与 `git diff --check` 通过。

- `python -m unittest discover -s tests -v`：137 项测试全部通过；新增画廊更新的完整重排/归档/GID 晋升、status 5 混合文件名恢复，以及旧 sidecar 已归档但新 sidecar 未晋升时的恢复测试。

- `python -m unittest discover -s tests -v`：133 项测试全部通过；新增中间缺页全局页码、混合本地/在线预览、单格增量替换、单页按需落盘和阅读器下载速度测试。`compileall`、`git diff --check` 与 Qt offscreen 主窗口启动/关闭冒烟通过。
- 使用当前 `Database/database` 完整导入 12 个命名空间、43,751 条标签；主程序启动索引构建约 0.58 秒，`full` 与“全彩”查询分别约 0.2ms 与 0.01ms。新增 Markdown 转义、重复导入、schema v7、中英文片段、本地语法还原、多条件补全、历史持久化/裁剪/排序及两行候选测试。
- 新增 Misc 灰色、Doujinshi 红色类别属性与实际渲染像素测试，并校验上传者文案不再包含字段前缀。
- `python -m unittest discover -s tests -v`：83 项测试全部通过；`compileall`、`git diff --check` 及带 43,751 条共享标签索引的主窗口启动/快速关闭离屏冒烟通过。
- 新增星级 CSS 到数值评分、Minimal 无标签、默认/Extended 卡片构建、站点 `inline_set` 和视图配置同步测试；当前 ExHentai 首页实测 Compact/Extended 各返回 25 条且模式切换生效，Extended 的 25 条均含标签，Compact 的 25 条评分均为浮点数且无 CSS 样式泄漏，验证后恢复 Compact。
- 新增站点空容器自动首页、分站点状态/第 2 页恢复、刷新绕过内存缓存、并发数即时更新、磁盘缓存站点隔离、过期删除及二次加载零网络请求测试。
- 当前 ExHentai 配置真实在线冒烟：一页返回 25 个画廊，并发封面任务成功写入 24 个有效缓存文件；深浅主题切换正常且未输出凭据。
- Qt offscreen 主窗口启动、缩放、浅色/深色/跟随系统切换及关闭通过，默认本地资源页未提前激活在线首页请求。
- 新增用户爬虫 Compact HTML 解析、`f_search` 注入、字段转换、前后页游标、异域 URL 拒绝以及在线页面浅色/深色渲染测试。
- 使用当前本机配置对真实在线列表做只读验证：返回 25 条结果、存在下一页游标，且首张远程封面成功取得 18,996 字节；验证过程未输出 Cookie/Token。
- `python -m compileall -q main.py app eh_tool_refactored.py`
- `git diff --check`
- Qt offscreen 主窗口创建与退出冒烟测试
- 浅色/深色主题切换渲染检查
- 漫画详情标签分组、裸标签去重、胶囊控件生成及 light/dark 配色渲染检查
- 三类标签右键删除、取消确认保护、未分类保护、分类删除后漫画回到未分类、播放列表成员清理、归类子树级联和外部 schema 不变测试
- 单项/批量移至未分类、目标 `DOWNLOADS.LABEL` 清空、`DOWNLOAD_LABELS` 标签保留及外部 schema 不变测试
- 漫画子导航、视频路由和全部占位页切换检查
- 外部 `eh.db` 常规浏览与读取校验哈希前后一致性检查
- `python -m unittest discover -s tests -v`
- 在线 provider 契约、Cookie 隐藏、代理/超时配置和界面 Worker 自动化测试
- RSViewer schema v1→v5 迁移、十六进制 `0000008f`→索引 143、sidecar 导入、自有记录优先、缺失/损坏 sidecar、进度文案及预览点击页码测试
- 真实 `.ehviewer` `0000000f`→第 16 页导入及详情预览第 3 张直达阅读验证；外部 `eh.db` 与 `.ehviewer` 哈希前后一致
- 21,389 条真实库元数据加自有进度批量读取约 0.483 秒，列表阶段页面路径为 0 且未逐本扫描 `.ehviewer`
- 阅读器页内/全局设置即时同步、四向翻页、点击图片后方向键、长图滚动到底换页及自动翻页计时器测试
- 阅读设置弹窗浅色/深色/浅色连续切换的渲染像素检查（深色 `#202020`、浅色 `#f3f3f3`）
- 漫画阅读器当前页/相邻页预读、翻页和全屏状态自动化测试，以及主窗口隐藏/恢复导航与标题栏的 Qt offscreen 冒烟测试
- 全屏上下边缘分别触发控制栏、中央鼠标活动和键盘翻页不唤出、透明页码持续显示及窗口模式恢复自动化测试
- 2000 页详情预览仅创建当前 40 张、最后一页真实页码点击、添加时间升降序、三类右键菜单及外部数据源默认倒序测试
- schema v5 收藏/历史持久化、右键收藏切换、共享数据集合顺序、播放列表编排、树状归类、跨漫画前后切换和标签栏 30% 宽度上限测试
- 网格/列表卡片在未开启复选时右键不触发详情、仍发送上下文菜单请求的鼠标事件测试
- 真实漫画首本 20 页读取验证：首屏解码成功、相邻 3 页缓存有效，外部 `eh.db` 校验哈希前后一致
- 阅读器在浅色、深色和跟随系统主题下的窗口/全屏切换与快速退出检查
- 真实漫画首本 20 页验证：点击图片后按方向键翻页、适应宽度、背景色即时更新、页内设置面板、三种主题和全屏往返均正常，外部 `eh.db` 哈希不变
- 21,389 条真实 EhViewer 数据与 Q: 漫画目录性能测试：列表元数据约 0.42 秒、首屏约 1.33 秒、列表阶段页面路径为 0
- 默认首页、父子导航、设置卡片、详情按需加载与快速退出的 Qt offscreen 冒烟测试
- 真实库当前页及后三页 240 个封面预加载验证：240 个均有效，预加载后第二页切换约 105ms
- 快捷键单键、`Ctrl+S` 组合键即时确认及 `Esc` 取消测试
- 相似画廊的章节/卷/话/前后篇与元数据噪声剥离、长标题容错、短标题防误报、右键结果模式及普通搜索恢复测试；21,389 条真实库首次后台全量比较约 3.3 秒，缓存后约 1.1 秒
